from pathlib import Path


class DataLoader:

    def __init__(self, dataset_path):
        self.dataset_path = Path(dataset_path)

    def load_documents(self):

        documents = []

        for file_path in self.dataset_path.glob("*.txt"):

            with open(file_path, "r", encoding="utf-8") as file:

                documents.append(
                    {
                        "document_name": file_path.name,
                        "text": file.read()
                    }
                )

        return documentsfrom pathlib import Path


class DataLoader:

    def __init__(self, dataset_path):
        self.dataset_path = Path(dataset_path)

    def load_documents(self):

        documents = []

        for file_path in self.dataset_path.glob("*.txt"):

            with open(file_path, "r", encoding="utf-8") as file:

                documents.append(
                    {
                        "document_name": file_path.name,
                        "text": file.read()
                    }
                )

        return documentsfrom pathlib import Path


class DataLoader:

    def __init__(self, dataset_path):
        self.dataset_path = Path(dataset_path)

    def load_documents(self):

        documents = []

        for file_path in self.dataset_path.glob("*.txt"):

            with open(file_path, "r", encoding="utf-8") as file:

                documents.append(
                    {
                        "document_name": file_path.name,
                        "text": file.read()
                    }
                )

        return documentsfrom pathlib import Path


class DataLoader:

    def __init__(self, dataset_path):
        self.dataset_path = Path(dataset_path)

    def load_documents(self):

        documents = []

        for file_path in self.dataset_path.glob("*.txt"):

            with open(file_path, "r", encoding="utf-8") as file:

                documents.append(
                    {
                        "document_name": file_path.name,
                        "text": file.read()
                    }
                )

        return documentsfrom pathlib import Path


class DataLoader:

    def __init__(self, dataset_path):
        self.dataset_path = Path(dataset_path)

    def load_documents(self):

        documents = []

        for file_path in self.dataset_path.glob("*.txt"):

            with open(file_path, "r", encoding="utf-8") as file:

                documents.append(
                    {
                        "document_name": file_path.name,
                        "text": file.read()
                    }
                )

        return documentsfrom pathlib import Path


class DataLoader:

    def __init__(self, dataset_path):
        self.dataset_path = Path(dataset_path)

    def load_documents(self):

        documents = []

        for file_path in self.dataset_path.glob("*.txt"):

            with open(file_path, "r", encoding="utf-8") as file:

                documents.append(
                    {
                        "document_name": file_path.name,
                        "text": file.read()
                    }
                )

        return documentsfrom pathlib import Path


class DataLoader:

    def __init__(self, dataset_path):
        self.dataset_path = Path(dataset_path)

    def load_documents(self):

        documents = []

        for file_path in self.dataset_path.glob("*.txt"):

            with open(file_path, "r", encoding="utf-8") as file:

                documents.append(
                    {
                        "document_name": file_path.name,
                        "text": file.read()
                    }
                )

        return documentsfrom pathlib import Path


class DataLoader:

    def __init__(self, dataset_path):
        self.dataset_path = Path(dataset_path)

    def load_documents(self):

        documents = []

        for file_path in self.dataset_path.glob("*.txt"):

            with open(file_path, "r", encoding="utf-8") as file:

                documents.append(
                    {
                        "document_name": file_path.name,
                        "text": file.read()
                    }
                )

        return documentsfrom pathlib import Path


class DataLoader:

    def __init__(self, dataset_path):
        self.dataset_path = Path(dataset_path)

    def load_documents(self):

        documents = []

        for file_path in self.dataset_path.glob("*.txt"):

            with open(file_path, "r", encoding="utf-8") as file:

                documents.append(
                    {
                        "document_name": file_path.name,
                        "text": file.read()
                    }
                )

        return documentsfrom pathlib import Path


class DataLoader:

    def __init__(self, dataset_path):
        self.dataset_path = Path(dataset_path)

    def load_documents(self):

        documents = []

        for file_path in self.dataset_path.glob("*.txt"):

            with open(file_path, "r", encoding="utf-8") as file:

                documents.append(
                    {
                        "document_name": file_path.name,
                        "text": file.read()
                    }
                )

        return documentsfrom pathlib import Path


class DataLoader:

    def __init__(self, dataset_path):
        self.dataset_path = Path(dataset_path)

    def load_documents(self):

        documents = []

        for file_path in self.dataset_path.glob("*.txt"):

            with open(file_path, "r", encoding="utf-8") as file:

                documents.append(
                    {
                        "document_name": file_path.name,
                        "text": file.read()
                    }
                )

        return documents